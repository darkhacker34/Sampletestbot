services:
  - type: web
    name: samplevideobot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - key: PORT
        value: "5000"
